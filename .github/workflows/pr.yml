on: pull_request_target

# checkout head_ref
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
      - name: Build and Test
        run: |
          echo "Test"
          git status
          git branch --show-current
          ls -la